Инструкция по использованию приложения для генерации этикеток WB
1. Подготовка данных в Excel
Перед загрузкой в приложение необходимо подготовить файл Excel (xlsx) с правильной структурой.

Шаг 1: Скачивание файлов с Wildberries
Файл 1 — "Лист сборки" (основной файл для этикеток)

Скачайте его из личного кабинета WB (раздел "Документы" → "Сборка").

В нём должны быть колонки: "Баркод", "Наименование", "Цвет", "Размер" (если есть).

Файл 2 — Файл с артикулами (например, из "Отчёта по продажам" или "Остатков")

В нём должны быть колонки: "Наименование", "Артикул WB".

Шаг 2: Добавление колонки "Артикул" в основной файл
Откройте Файл 1 (Лист сборки).

Добавьте новую колонку "Артикул" (например, рядом с "Наименование").

Шаг 3: Синхронизация артикулов через ВПР (VLOOKUP)
Скопируйте данные из Файла 2 (артикулы) в отдельный лист Файла 1.

Используйте формулу ВПР, чтобы подтянуть артикулы:

excel
=ВПР([Наименование]; [Диапазон_поиска_в_Файле2]; Номер_колонки_с_артикулом; ЛОЖЬ)
Пример:

excel
=ВПР(A2; Лист2!A:B; 2; ЛОЖЬ)
Где:

A2 — ячейка с наименованием товара в Файле 1.

Лист2!A:B — диапазон в Файле 2, где A — наименование, B — артикул.

2 — номер колонки с артикулом в Файле 2.

Растяните формулу на все строки.

Если артикулы не находятся (#Н/Д), проверьте:

Совпадают ли наименования в обоих файлах (возможны опечатки).

Если названия различаются, можно использовать частичное совпадение (ПОИСКПОЗ + ИНДЕКС).

Скопируйте полученные артикулы и вставьте значения (чтобы убрать формулы).

Шаг 4: Проверка данных
Убедитесь, что в файле есть:

Баркод (обязательно)

Наименование (обязательно)

Артикул (обязательно)

Цвет/Размер (если нужны на этикетке)

Сохраните файл в формате .xlsx.

2. Загрузка файла в приложение
Откройте приложение и перейдите на страницу загрузки.

Выберите режим генерации:

📦 Массовая печать — создаст один PDF со всеми этикетками.

📄 Шаблоны — создаст отдельные PDF для каждого товара.

Загрузите подготовленный файл (.xlsx).

3. Настройка этикеток
Укажите строку с заголовками (обычно строка 1, но может отличаться).

Выберите колонки, которые будут на этикетке:

Обязательные: "Баркод", "Наименование", "Артикул".

Дополнительные: "Цвет", "Размер" (если нужны).

Настройте соответствия:

Какая колонка → какой элемент этикетки (например, "Баркод" → штрихкод).

Проверьте данные и при необходимости отредактируйте:

Количество этикеток для каждого товара (по умолчанию ×2).

Цвет/размер (если нужно изменить).

4. Генерация PDF
Нажмите "Сгенерировать PDF".

Готово!

В режиме массовой печати скачается один файл labels.pdf.

В режиме шаблонов файлы сохранятся в папке patterns/.

🔹 Важные нюансы
Если штрихкод не отображается, проверьте, что в файле он указан без пробелов.

Для русских названий в Excel используйте UTF-8 (чтобы не было "кракозябр").

Если артикулы не подтягиваются, попробуйте очистить форматирование в Excel.

📌 Пример структуры файла
Баркод	Наименование	Артикул	Цвет	Размер
12345678901	Футболка оверсайз	789123	Чёрный	XL
98765432109	Джинсы скинни	456789	Синие	42
